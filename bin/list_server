#!/bin/bash

# Função para listar os servidores ativos
list_active_servers() {
  echo "Active Rails servers:"
  ps aux | grep 'rails server' | grep -v grep

  echo "Active Vite servers:"
  ps aux | grep 'vite' | grep -v grep
}

# Listar os servidores ativos
list_active_servers