version: '27.3'
services:
  backend:
    build:
      context: ../Backend
      dockerfile: ../docker/Dockerfile.backend
    ports:
      - "4000:4000"
    volumes:
      - ../Backend:/app
      - /app/node_modules
      - ../Backend/Data:/app/Data
    environment:
      - NODE_ENV=development
    command: ["node", "server.mjs"]

  frontend:
    build:
      context: ../Frontend
      dockerfile: ../docker/Dockerfile.frontend
    ports:
      - "3000:3000"
    volumes:
      - ../Frontend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development

  database:
    image: postgres:14-alpine
    volumes:
      - db_data_pg_14:/var/lib/postgresql/data
      - ../Backend/Data/initdb/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ../Backend/Data/initdb/create_user.sql:/docker-entrypoint-initdb.d/create_user.sql
      - ../Backend/Data/initdb/init.sh:/docker-entrypoint-initdb.d/init.sh
    ports:
      - "5444:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: senha_segura
      POSTGRES_DB: dripstore

volumes:
  db_data_pg_14: